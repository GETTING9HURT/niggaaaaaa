# This file is configured to allow the Netlify Next.js
# plugin to handle the build and deployment of your site.
# The plugin automatically configures the correct settings.

[build]
  # The build command is automatically handled by the plugin.
  # The publish directory is also handled by the plugin.

[[plugins]]
  # This is the essential plugin for deploying Next.js on Netlify.
  package = "@netlify/plugin-nextjs"
