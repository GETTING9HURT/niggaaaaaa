rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read access to remedy photos
    match /remedy-photos/{allPaths=**} {
      allow read;
      // Allow write access only for authenticated users
      allow write: if request.auth != null;
    }
  }
}
